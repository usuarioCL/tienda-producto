<%- include('partials/header') %>

<form action="/create" id="formulario-registro" autocomplete="off" method="post">
  <div class="card mt-2">
    <div class="card-header"><strong>Registro de Producto</strong></div>

    <div class="card-body">
      
      <div class="row g-2">
        <div class="col-md-6 mb-2">
          <div class="form-floating">
            <select name="categorias" id="categorias" class="form-select" required>
              <option value="" selected disabled>Seleccione</option>
              <!-- Las categorías se cargan desde la BD -->
              <% categorias.forEach(element => { %>
                <option value="<%= element.idcategoria %>"><%= element.categoria %></option>
              <% }) %>
            </select>
            <label for="categorias">Categoría</label>
          </div>
        </div>

        <div class="col-md-6 mb-2">
          <div class="form-floating">
            <input type="text" id="nombre" name="nombre" class="form-control" placeholder="Nombre" required>
            <label for="nombre">Nombre</label>
          </div>
        </div>
      </div>

      <div class="row g-2">
        <div class="col-md-6 mb-2">
          <div class="form-floating">
            <textarea id="descripcion" name="descripcion" class="form-control" placeholder="Descripción" required></textarea>
            <label for="descripcion">Descripción</label>
          </div>
        </div>

        <div class="col-md-3 mb-2">
          <div class="form-floating">
            <input type="number" id="precio" name="precio" class="form-control" placeholder="Precio" required step="0.01">
            <label for="precio">Precio</label>
          </div>
        </div>

        <div class="col-md-3 mb-2">
          <div class="form-floating">
            <input type="number" id="stock" name="stock" class="form-control" placeholder="Stock" required min="1">
            <label for="stock">Stock</label>
          </div>
        </div>
      </div>
    </div>

    <div class="card-footer">
      <button type="submit" class="btn btn-sm btn-primary">Guardar</button>
      <a href="/" class="btn btn-sm btn-secondary">Cancelar</a>
    </div>
  </div>
</form>

<!-- Incluir el catálogo de productos aquí -->
<%- include('catalogo') %> <!-- Esto incluirá el archivo catalogo.ejs -->

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const formulario = document.querySelector("#formulario-registro")

    formulario.addEventListener("submit", (event) => {
      event.preventDefault()

      Swal.fire({
        title: 'Producto',
        text: '¿Está seguro de registrar el producto?',
        icon: 'question',
        footer: 'SENATI - Ingeniería de Software',
        confirmButtonText: 'Aceptar',
        confirmButtonColor: '#2980b9',
        showCancelButton: true,
        cancelButtonText: 'Cancelar',
        cancelButtonColor: '#c0392b'
      }).then((result) => {
        if (result.isConfirmed){
          formulario.submit()
        }
      })

    })
  })
</script>

<%- include('partials/footer') %>
